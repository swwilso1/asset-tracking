/**
* The alerts api return array of the alerts generated by the decision table and logged in the system
* 
* @module alerts
* @param {String} [id] the device id /Optional 
* @return{Array} array of alerts that match the id if the id is sent , all the alerts otherwise
*
*/

var deviceManager=require("asset-tracking/entities/deviceManager");
//Ingest the request params or data
var requestParams = request.body || request.parameters;

try {
    if(requestParams.queryFilter){
        return deviceManager.searchAlerts(requestParams.queryFilter, requestParams.resultsPerPage, requestParams.pageNumber);
    }
    return deviceManager.getAllAlerts(requestParams.resultsPerPage, requestParams.pageNumber);
}catch(ex){
    throw ex
}
    
